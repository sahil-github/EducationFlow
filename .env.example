# Base URL of your backend API server (no trailing slash, no /api suffix)
# The Axios client in src/api/api.js appends /api/... to every endpoint path.
VITE_API_BASE_URL=http://localhost:5000

# Application display name (used in page titles, etc.)
VITE_APP_NAME=EduFlow
